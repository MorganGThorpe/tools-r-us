<div class="d-flex">
  <div class="mx-5 w-25">
    <%=  simple_form_for(@tool) do |t| %>
      <%= t.input :name %>
      <%= t.input :photo, as: :file%>
      <%= t.input :price_per_day %>
      <%= t.input :description %>
      <%= t.input :category, collection: Tool::CATEGORIES %>
      <%= t.submit submit_text %>
    <% end %>
  </div>
</div>
